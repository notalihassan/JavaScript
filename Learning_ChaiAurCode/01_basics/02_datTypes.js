"use strict"; // treat all JS code as newer version

// alert( 3 + 3) // we are using nodejs, not browser. it will work on the browser
// we can also run this in the nodejs we can learn after some time

console.log(3 
    +
     3) // code readability should be low

console.log("Hitesh")


let name = "hitesh"
let age = 18
let isLoggedIn = false
let state;

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined => 
// symbol => unique (used in React,Figma)
// object

/*to check variable data type use typeof function*/
//Syntax:
console.log(typeof null);
//OR
console.log(typeof(null));


/*Interview Question*/
console.log(typeof undefined); //output: undefined
console.log(typeof null); //output: object